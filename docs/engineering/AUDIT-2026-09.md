# CortexForge Repository Audit — Phase 0

**Audited commit:** `1eb4e41`
**Date:** 2026-09-08
**Method:** Full source read of `src/`, `tests/`, `migrations/`, `apps/web/src/`; execution-path
tracing (definition → call site → reachability → persistence → downstream effect → test);
`pytest` executed against Python 3.12 (50 passed, 1 failed).

This audit classifies *capabilities*, not files. The existence of a class is never treated as
evidence that a capability works. Each row answers: where is it defined, where is it called, is it
reachable, does it persist state, does it change downstream behaviour, is it tested meaningfully.

---

## 1. Classification summary

| # | Capability | Status | Evidence |
|---|---|---|---|
| 1 | Repository scan (tree-sitter, 5 languages) | **IMPLEMENTED** | `code_intelligence/scanner.py:112`; persists `CodeEntity`/`Relationship`; reachable from CLI, API, MCP, jobs; covered by `tests/integration/test_scanner_and_graph.py`. |
| 2 | Incremental scan by git diff | **IMPLEMENTED** | `scanner.py:138-180` uses `last_indexed_commit`→`HEAD` diff, deletes entities for `D` files. Real. |
| 3 | Incremental **graph** update | **PARTIAL** | Entities are updated incrementally, but relationships for a changed file are deleted and re-derived; cross-file edges pointing *into* the changed file are rebuilt only if the other side is rescanned. No relationship generation counter. |
| 4 | AST semantic diff | **IMPLEMENTED** | `treesitter/semantic_diff.py:105` produces `SYMBOL_ADDED/REMOVED/RENAMED/MOVED/SIGNATURE_CHANGED/BODY_CHANGED`; tested in `tests/unit/test_semantic_diff.py`. |
| 5 | Symbol identity / lineage | **PARTIAL** | `are_symbols_lineage_match()` (`semantic_diff.py:79`) detects rename in-memory and re-anchors evidence, but **no lineage is persisted**. There is no logical-symbol identity that survives restart; a second process cannot answer "what was `foo()` renamed to". |
| 6 | Change → memory propagation | **PARTIAL / MONOLITHIC** | `change_propagator.py:77` is a single 400-line function that mixes diffing, graph traversal, memory mutation, architecture evaluation and persistence. Decisions (stale/reanchor/invalidate) exist **only as report strings** — no decision record is persisted, so the reasoning is unauditable and unreplayable. |
| 7 | ChangeSet / FileChange / SymbolChange persistence | **BROKEN (correctness)** | `change_propagator.py:~420`: `base_commit_sha` is set to the *base* but `target_commit_sha` to `project.last_indexed_commit` — inverted/meaningless; every call creates a **new** `ChangeSet` with no idempotency key, so re-running the same analysis duplicates the changeset (violates §37). All `FileChange.change_type` are hardcoded `"MODIFIED"` regardless of add/delete/rename. |
| 8 | Memory lifecycle FSM | **IMPLEMENTED** | `memory/lifecycle.py` enforces a real transition graph, rejects `INVALIDATED→ACTIVE`, writes `MemoryVersion` audit rows. Tested (`tests/unit/test_memory_lifecycle.py`). Missing `REVIEW_REQUIRED` state (§7/§43). |
| 9 | Claim / Proposition model | **MISSING** | No `Claim`, `ClaimEvidence`, `VerificationPolicy`, `VerificationRun`, `VerificationResult` anywhere in `src/`. Verification operates on whole memories, so a memory is atomically "true" or "stale" — §4 not implemented at all. |
| 10 | Evidence model | **PARTIAL** | `MemoryEvidence` stores file/symbol/lines/hashes — good. But: only **positive** evidence (no `CONTRADICTS`/`WEAKENS`/`SUPERSEDES` relation), no `evidence_type` taxonomy (CODE/TEST/CONFIG/API_CONTRACT/…), no `authority`, no `branch`/`workspace`, no per-evidence verification state. |
| 11 | Authority model | **PARTIAL / UNUSED** | `security/trust.py` defines 7 trust levels + numeric scores; `get_trust_authority` is called by **no production code** — its only caller is `tests/unit/test_security_redactor.py:70`, which asserts the constants are ordered (an import-shaped test per §58). The real authority signal is the duplicate `SOURCE_AUTHORITY_WEIGHTS` table in `memory/confidence.py`. No `LLM_GENERATED`, `REVIEW_CONFIRMED`, `USER_CONFIRMED` distinction; no ordinal ranking used in conflict resolution. |
| 12 | Explainable confidence | **PARTIAL** | `ConfidenceScorer` (`memory/confidence.py`) is genuinely multi-factor and explainable — but its `breakdown`/`explanation` is **never persisted**, and it is bypassed by the verification path. |
| 13 | Confidence inflation | **BROKEN (spec violation §8)** | `memory/verification.py:158`: `memory.confidence = min(1.0, memory.confidence + 0.05)` on every successful verification. Re-verifying identical, unchanged evidence N times drives confidence to 1.0. This is exactly the anti-pattern §8 forbids. |
| 14 | Automated verification | **PARTIAL** | `MemoryVerificationEngine` does real work (AST re-parse + snippet sha256 comparison) and re-anchors line numbers. But there is no policy, no run record, no result record, no `PARTIALLY_VERIFIED`/`CONFLICTED`/`NOT_APPLICABLE`/`UNKNOWN` — the only outcomes are ACTIVE or STALE. §30 ("UNKNOWN is a valid answer") is not representable. |
| 15 | Conflict resolution | **PARTIAL** | `conflict_resolver.py` uses embedding similarity + negation heuristics, then arbitrates on `confidence`/recency. Arbitration does **not** consult an authority hierarchy, so an LLM-generated memory can supersede a user decision if its confidence happens to be higher (§6 violation). |
| 16 | Temporal cognition | **PARTIAL** | `valid_from_commit`/`valid_to_commit`/`valid_from_time`/`valid_to_time` columns exist on `Memory` but are **never written and never read** by any query — grep shows zero assignments outside model definition. Historical-truth vs conflict (§18) cannot be distinguished. |
| 17 | Branch / worktree / workspace cognition | **MISSING** | No `branch` or `workspace` column on `Memory`, `MemoryEvidence`, or `ChangeSet`. `COMMITTED_TRUTH` vs `WORKING_TREE_STATE` is expressed only as `ChangeSet.is_working_tree`; uncommitted observations become durable memories with no marking (§19 violation). |
| 18 | Merge / rebase reconciliation | **MISSING** | No handling of merge, rebase, cherry-pick, force-push or branch deletion anywhere. |
| 19 | Hybrid retrieval | **PARTIAL / MISREPRESENTED** | `retrieval/engine.py:59` loads **every** memory *and* **every** code entity of the project into Python, fits BM25 in Python and computes cosine in Python per query — O(N) per request. pgvector is a declared dependency but no vector column, index or `<=>` operator is used anywhere (grep: zero hits for `pgvector`/`Vector` in `src/`). Documentation calling this "production hybrid retrieval" is drift (§21, §46). |
| 20 | MMR diversity rerank | **IMPLEMENTED** | `engine.py:199`, tested in `tests/unit/test_retrieval_bm25_mmr.py`. |
| 21 | Embedding space management | **PARTIAL** | `embedding_model`/`embedding_version` are persisted per memory. But nothing prevents mixing spaces at query time, there is no reindex/migration path, and no `quality_class` distinguishing `LOCAL_DETERMINISTIC_HASH` from a real semantic embedding (§22). |
| 22 | Env var contract | **BROKEN** | `.env.example` documents `CORTEX_EMBEDDINGS_PROVIDER`; `embeddings/provider.py:170` reads `CORTEX_EMBEDDING_PROVIDER`. Setting the documented variable silently has no effect — configuration/documentation drift of exactly the kind §46 says the system should detect. |
| 23 | No silent mock fallback | **PARTIAL** | `OpenAIProvider.generate` and `OpenAIEmbeddingProvider.embed_batch` raise only when `CORTEX_ENV`/`ENVIRONMENT` == `production`; the default is `development`, so an unset env var in a real deployment silently returns `MockLLMProvider` output that then flows into durable memory (§24 violation in practice). |
| 24 | LLM authority boundary | **BROKEN (spec violation §23/§26)** | `memory/consolidation.py:~150`: LLM text is parsed and written directly as a `Memory` with `status=ACTIVE, confidence=0.90`, and the source episodes are `ARCHIVED` in the same transaction. LLM synthesis → ACTIVE → archive-sources is the precise pipeline §26 prohibits. No candidate stage, no dedup against existing lessons, no evidence validation, no verification. |
| 25 | Consolidation idempotency | **PARTIAL (accidental)** | Re-running does not duplicate only because sources were archived and the query filters on `ACTIVE`. There is no cluster fingerprint, so a new similar episode regenerates a near-duplicate lesson (§25/§37). |
| 26 | Memory deduplication | **MISSING** | No canonical claim key, no logical dedup. Two phrasings of one fact create two memories. |
| 27 | Test intelligence | **PARTIAL** | `TestRun`/`TestCaseResult` persist real data including `failure_signature` and affected files/symbols. But causal attribution (`INTRODUCED_BY`/`FIXED_BY`/`REGRESSED_BY`/`FLAKY`/`UNRELATED`) is absent, and no historical flakiness is tracked (§15). |
| 28 | Failure intelligence | **IMPLEMENTED** | `FailureEpisode` + `FixAttempt` with signature, normalized trace, root cause, affected files/symbols; written by `agent/failure_intelligence.py`; retrievable via MCP `task_record_failure` / `memory_get_failures`. |
| 29 | Success intelligence | **MISSING** | No `SuccessEpisode` or equivalent. The system learns only from failure (§17). |
| 30 | Memory usefulness learning | **MISSING** | No retrieval event log: what was returned, what was selected, what was used, task outcome. Retrieval policies therefore cannot be evaluated (§27). |
| 31 | Historical task similarity | **PARTIAL** | MCP `task_find_similar` exists; scoring is term-overlap based, not embedding + files + symbols + failure signatures (§28). |
| 32 | Context composer + budget | **IMPLEMENTED** | `retrieval/composer.py` enforces profile token budgets, records `why_selected` and exclusion reasons. Genuinely good; tested. |
| 33 | UNKNOWN as an answer | **MISSING** | No code path returns UNKNOWN. Absence of evidence yields ACTIVE-with-lower-confidence or STALE (§30). |
| 34 | Agent telemetry | **PARTIAL** | `AgentTask`/`AgentEvent` are real raw event infrastructure. No typed projections (TASK_STARTED, TOOL_CALLED, …) and no per-agent adapters (§31). |
| 35 | MCP server | **IMPLEMENTED (thin)** | 30 tools in `apps/mcp/server.py`, all delegating to domain services. Good layering. But it exposes unrestricted `memory_create` writing durable memory directly — §32 asks for `observe/propose/verify/approve`. |
| 36 | REST API | **IMPLEMENTED** | Routes delegate to the same services as MCP; no duplicated business logic. |
| 37 | CLI | **IMPLEMENTED** | `apps/cli/main.py`, 558 lines, calls domain services. |
| 38 | GitHub webhook ingestion | **BROKEN (idempotency)** | `routes/github.py:38` verifies HMAC correctly but ignores `X-GitHub-Delivery`. A redelivered push re-runs propagation and re-creates a `ChangeSet`, duplicating cognitive changes (§33/§37 violation). Also: missing secret ⇒ signature check returns `True`. |
| 39 | Git hooks (post-commit/merge/checkout) | **MISSING** | No hook installation or handling. |
| 40 | Background jobs | **PARTIAL** | `jobs/manager.py` is an in-process `asyncio.Task` registry with an in-memory dict. Nothing is persisted, so no job survives a restart; §38 (crash consistency, resumability, leases, attempt IDs) is not met. Redis is referenced but unused. |
| 41 | Cache correctness | **UNUSED** | `jobs/manager.py:125 CacheManager` is a generic in-process TTL dict. Grep shows **no production caller** — only `tests/unit/test_jobs_and_caching.py` exercises it. Since nothing is cached, §40's generation-keying requirement is unimplemented rather than wrong. |
| 42 | Cognitive snapshots | **PARTIAL / MISREPRESENTED** | `CognitiveSnapshot` stores **counts and generation integers only**. §35 explicitly says counts are insufficient. No memory-version set, no verification state, no rule set, no policy versions. |
| 43 | Deterministic replay | **BROKEN (misrepresented)** | `snapshots.py:106 replay_state_at_commit()` ignores the commit entirely and composes context from the **current** memory table. It answers "what do you believe now", labelled as "what did you believe at commit X". |
| 44 | Architecture rules | **PARTIAL** | `ArchitectureRule` + `RuleViolation` are real and evaluated in `architecture/invariants.py` during propagation, with violations persisted. Only MUST_NOT (forbidden source→target regex) is expressible — no MUST/SHOULD/ONLY_IF/REQUIRES, no authority, evidence, version, or source on rules (§13). |
| 45 | Config / contract intelligence | **MISSING** | Scanner parses only code extensions. `.env`, Docker, compose, CI YAML, OpenAPI, schemas and migrations are not indexed and cannot participate in change impact (§14). |
| 46 | Secret redaction | **IMPLEMENTED** | `security/redactor.py` covers private keys, OpenAI/AWS/GitHub/Slack tokens, JWTs, DB URLs, generic passwords; tested in `tests/unit/test_security_redactor.py`. Applied at memory creation. |
| 47 | Path traversal defence | **IMPLEMENTED** | `security/path_safety.py`, used by scanner/verification via `os.path.realpath` containment. |
| 48 | Prompt-injection / memory-poisoning defence | **PARTIAL** | Repository text is redacted but not trust-tagged; there is no `UNTRUSTED` authority applied to content read from the repo, and no adversarial test suite (§41/§51). |
| 49 | Multi-project isolation | **IMPLEMENTED** | Every domain query filters on `project_id`. No cross-project leak found by inspection. Not covered by a property test. |
| 50 | Approval workflow | **MISSING** | No `REVIEW_REQUIRED`/`APPROVED` path for security, architecture or high-impact memories (§43). |
| 51 | Audit log | **PARTIAL** | `MemoryVersion` is a real per-memory audit trail. There is no system-wide audit log for rule changes, provider changes, project configuration or approvals, and no actor/trace correlation (§44). |
| 52 | Observability | **PARTIAL** | `observability/metrics.py` is an in-process counter/histogram registry, tested. No trace IDs propagated through requests, no OpenTelemetry export despite `CORTEX_TELEMETRY` in `.env.example`. |
| 53 | Benchmark harness | **BROKEN (hardcoded results — §49 violation)** | `evaluation/runner.py:280-420`: Modes A–F return **literal constants** — `tests_passed=3`, `latency_ms=45.0`, `retrieval_precision=0.20/0.55/0.75`, `provenance_correctness=0.30`, `false_stale_rate=0.15`. Only Mode G composes real context. `file_token_map` even falls back to a fabricated 8-file dictionary (`services/auth.py: 720`, …) when the repository is small. These fabricated numbers are surfaced through `/evaluation` to the dashboard as measured results. |
| 54 | Mutation benchmark | **PARTIAL** | `evaluation/mutations.py` defines real mutations with expected cognitive outcomes and is tested (`tests/unit/test_mutation_benchmark.py`) — the strongest part of the evaluation suite. |
| 55 | Adversarial / temporal benchmark | **MISSING** | No wrong/stale/duplicate/malicious memory suite (§51); no A-true / B-false / C-new temporal suite (§52). |
| 56 | Property-based testing | **MISSING** | No `hypothesis` dependency; none of the §53 properties are asserted. |
| 57 | Contract testing | **MISSING** | No OpenAPI↔routes, models↔migrations, frontend↔backend, or docs↔capability checks (§54). |
| 58 | Frontend | **IMPLEMENTED (React+TS+Vite)** | 11 components, real API calls via `apps/web/src/api.ts`, TanStack-style fetching. Correct stack per §48 — no Next.js migration needed. **However** `EvaluationHarness.tsx` and `TokenEconomics.tsx` render the fabricated benchmark numbers from #53 as if measured. |
| 59 | Migrations | **PARTIAL** | Two Alembic revisions exist and match the models at this commit, but there is no models↔migrations drift test, and no DB-level `CHECK` constraints for confidence/importance ranges or unique idempotency keys (§47). |
| 60 | Test suite | **PARTIAL** | 51 tests, 50 pass. Many are meaningful (semantic diff, lifecycle FSM, symbol-level invalidation, redaction, mutations). `tests/integration/test_scanner_and_graph.py:176` **fails** — it asserts `GET /` returns 200, which requires a built frontend `dist/` that CI does not build before pytest. Unconditional assertion on an optional artifact. |

---

## 2. The ten questions, applied to the load-bearing capabilities

| Capability | Defined | Called | Reachable | Persists | Affects downstream | Tested | Meaningful test | Deterministic | Works on real repo change | Production-safe |
|---|---|---|---|---|---|---|---|---|---|---|
| AST semantic diff | ✅ | ✅ | ✅ | ✅ (`SymbolChange`) | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| Symbol lineage | ✅ | ✅ | ✅ | ❌ | ✅ (in-memory only) | ✅ | ⚠️ in-memory only | ✅ | ⚠️ single-process | ❌ |
| Memory verification | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ⚠️ no policy/result | ⚠️ confidence drifts up | ✅ | ❌ (§8) |
| Claim evaluation | ❌ | — | — | — | — | — | — | — | — | — |
| Consolidation | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ❌ asserts counts, not safety | ⚠️ | ⚠️ | ❌ (§23/§26) |
| Hybrid retrieval | ✅ | ✅ | ✅ | ❌ (no event log) | ✅ | ✅ | ✅ for ranking | ✅ | ✅ | ⚠️ O(N)/query |
| Replay | ✅ | ✅ | ✅ | ❌ | ❌ (ignores commit) | ✅ | ❌ asserts a dict shape | ✅ | ❌ | ❌ |
| Benchmark | ✅ | ✅ | ✅ | ✅ (JSON artifacts) | ✅ (dashboard) | ✅ | ❌ asserts constants | ✅ trivially | ❌ | ❌ (§49) |
| Webhook ingestion | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ⚠️ no replay test | ❌ non-idempotent | ✅ | ❌ (§33/§37) |

---

## 3. Root-cause themes

1. **No proposition layer.** Everything above verification (confidence, conflict, staleness,
   reconciliation) operates on whole memories. A memory containing one true and one false
   statement has no representable state. This is the single largest structural gap and blocks
   §4–§10 simultaneously.
2. **Decisions are not records.** Reconciliation outcomes exist as strings in a returned
   dataclass. Nothing is persisted, so nothing is auditable, replayable, or measurable.
3. **Authority is declared but unused.** Two competing authority tables exist; the ordinal
   hierarchy that §6 requires is enforced nowhere.
4. **Truth is asserted where it should be measured.** Benchmarks, replay and snapshots present
   fabricated or current-state data as historical or measured fact — the exact failure mode
   §46/§49/§59 warn against.
5. **Idempotency is assumed, not enforced.** Changesets, webhook deliveries and consolidation
   have no keys or unique constraints.
6. **Confidence can be manufactured.** Repeated verification of unchanged evidence raises
   confidence without new information.

## 4. Remediation order (mapped to the specification's phases)

Ordered by "unblocks the most downstream correctness", not by size:

1. Authority + epistemic-state primitives (§6, §3) — prerequisite for everything else.
2. Claim / ClaimEvidence / VerificationPolicy / VerificationRun / VerificationResult (§4, §5, §7).
3. Remove confidence inflation; persist confidence components (§8).
4. Reconciliation engine producing persisted `MemoryDecision` records (§9).
5. Safe consolidation: LLM → CANDIDATE → dedup → evidence → verify → ACTIVE (§23, §25, §26).
6. `REVIEW_REQUIRED` state + approval workflow (§7, §43).
7. Success episodes, retrieval events, audit log (§17, §27, §44).
8. Idempotency keys and unique constraints: changesets, webhook deliveries, claims, decisions (§37).
9. Persisted symbol lineage (§11).
10. Snapshot content + honest replay (§35, §36).
11. Benchmark de-fabrication (§49).
12. DB-native retrieval, config/contract indexing, merge reconciliation, property/contract tests
    (§21, §14, §20, §53, §54) — larger, sequenced after the correctness core.
